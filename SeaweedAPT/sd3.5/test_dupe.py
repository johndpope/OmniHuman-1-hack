# test_dupe.py
import logging
logger = logging.getLogger("test")

def load_sd35_weights(model_path: str, device: str = "cuda"):
    logger.info(f"Loading weights from {model_path}")
    return {}

def load_sd35_weights(model_path: str, device: str = "cuda"):
    logger.info(f"Loading weights from {model_path}")
    return {}